<%# Resolves the tool's own result partial. Shared by the card and the result
    broadcast, so both render the same output. %>
<%= render tool_result_partial(tool_call), tool_call: tool_call, tool_result: tool_call.result %>
