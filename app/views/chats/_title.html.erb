<%# Lives on its own so ChatTitleJob can replace the header in place once the
    title has been generated. The id is the turbo-stream target. %>
<h1 id="<%= dom_id(chat, :title) %>" class="min-w-0 flex-1 truncate px-1 font-semibold"><%= chat.display_title %></h1>
